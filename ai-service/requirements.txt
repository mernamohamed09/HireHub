# CPU-only per spec: install torch first with
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# then run: pip install -r requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
transformers>=4.40
sentence-transformers>=3.0
torch>=2.2
numpy>=1.26
python-dotenv>=1.0
